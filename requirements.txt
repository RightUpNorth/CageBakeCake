# CageBakeCake runtime dependencies, pinned to the tested versions.
# Install into a virtualenv:  python -m pip install -r requirements.txt
#
# Until now the project had no manifest and deps lived ad-hoc in .venv; this
# records the set the code actually imports so the environment is reproducible.

# Core math / mesh
numpy==2.4.6
scipy==1.17.1            # trimesh vertex-normal (sparse) path

# USD interchange (the internal mesh format)
usd-core==26.5

# Viewport + Qt front end
pyvista==0.48.4
vtk==9.6.2
pyvistaqt==0.11.4
qtpy==2.4.3
PySide6==6.11.1

# Bake: ray casting + image IO
trimesh==4.12.2
embreex==4.4.0          # embree backend for the BVH ray caster
imageio==2.37.3

# Tests
pytest==9.0.3
